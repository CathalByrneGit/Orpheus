% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality.R
\name{orph_codelist_violations}
\alias{orph_codelist_violations}
\title{Values recorded outside the codelist that governs them}
\usage{
orph_codelist_violations(con, bundle = NULL, document_id = NULL)
}
\arguments{
\item{con}{A connection.}

\item{bundle}{A bundle. Defaults to the active one.}

\item{document_id}{Restrict to one document, or \code{NULL} for the corpus.}
}
\value{
A data frame of type, property, offending value and how often it occurs.
}
\description{
Some properties are governed by an OCDS codelist -- \code{procurement_procedure}
by the closed \code{procurementMethod} list, \code{role} by the open \code{partyRole} one.
A value outside the list is \strong{reported, not rejected}, for the same reason an
unrecognised property becomes a schema amendment rather than being dropped:
the mismatch is the signal. A model returning "Direct Award" instead of
\code{direct} means the prompt needs work; a genuine role the codelist lacks means
the codelist needs extending.
}
\details{
Concepts written against a codelist -- \code{direct_award} is \code{procurement_procedure = 'direct'} -- silently miss every row listed here, which is what makes this
worth checking rather than assuming.
}
