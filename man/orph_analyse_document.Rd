% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concepts.R
\name{orph_analyse_document}
\alias{orph_analyse_document}
\title{Generate the narrative analysis for a document}
\usage{
orph_analyse_document(
  con,
  document_id,
  tier = c("cloud", "local"),
  actor_id = NULL,
  opt_in = FALSE
)
}
\arguments{
\item{con}{A writable connection.}

\item{document_id}{Document identifier.}

\item{tier}{\code{"cloud"} (the default; this is the nuanced-reading pass) or \code{"local"}.}

\item{actor_id}{Actor triggering it.}

\item{opt_in}{Explicit cloud opt-in. Required when \code{tier = "cloud"}.}
}
\value{
A list with the evaluation id and the parsed result.
}
\description{
Interpretation, not extraction: a summary, risk level, key issues and
recommendations built from the instances already extracted. It reads
structured facts rather than the document text, which keeps what is sent to
a cloud model to the facts a person has already been able to see.
}
