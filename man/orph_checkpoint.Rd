% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{orph_checkpoint}
\alias{orph_checkpoint}
\title{Checkpoint the write-ahead log}
\usage{
orph_checkpoint(con, mode = c("PASSIVE", "TRUNCATE", "FULL", "RESTART"))
}
\arguments{
\item{con}{A writable connection.}

\item{mode}{\code{"PASSIVE"} never blocks; \code{"TRUNCATE"} also resets the WAL file
and is worth paying for after a batch of writes.}
}
\value{
Invisibly, the checkpoint result row.
}
\description{
Folds committed WAL pages back into the main database file.
}
\details{
This is not housekeeping -- it is load-bearing for the read path. With WAL
enabled, committed data lives in the \code{-wal} sidecar until a checkpoint. A
reader that opens the database with SQLite's \code{immutable=1} flag (which is
what \code{datasette --immutable} does) skips the WAL entirely and therefore sees
the database as of the last checkpoint -- on a freshly written store, that
means it sees nothing at all. Checkpointing after each write keeps every
reader current and stops the WAL growing without bound.
}
