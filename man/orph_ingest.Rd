% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{orph_ingest}
\alias{orph_ingest}
\title{Ingest a document}
\usage{
orph_ingest(
  con,
  path,
  actor_id = NULL,
  storage_root = "storage",
  filename = NULL,
  visibility = c("private", "link-view", "link-edit")
)
}
\arguments{
\item{con}{A writable connection.}

\item{path}{Path to the file to ingest.}

\item{actor_id}{Actor performing the ingest.}

\item{storage_root}{Directory for originals and page images.}

\item{filename}{Original filename, if different from \code{basename(path)}.}

\item{visibility}{\code{"private"}, \code{"link-view"} or \code{"link-edit"}.}
}
\value{
A list with the document id, page count, and whether this was a
duplicate of an existing document.
}
\description{
Extracts text, records one row per page, hashes the file for dedup, and
copies the original into the content-addressed store. Pages that yield too
little text to be real text are rendered to images and sent to the
configured OCR provider; with no provider configured they are recorded as
\code{needs_ocr} rather than being passed off as empty pages.
}
