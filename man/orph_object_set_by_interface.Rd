% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{orph_object_set_by_interface}
\alias{orph_object_set_by_interface}
\title{Query every object type that implements an interface, as one set}
\usage{
orph_object_set_by_interface(
  con,
  interface_id,
  bundle = NULL,
  document_id = NULL,
  include_rejected = FALSE,
  where = NULL,
  params = list()
)
}
\arguments{
\item{con}{A connection.}

\item{interface_id}{Interface identifier, e.g. \code{"Named"}.}

\item{bundle}{A bundle. Defaults to the active one.}

\item{document_id}{Restrict to one document, or \code{NULL} for the corpus.}

\item{include_rejected}{Include rows a reviewer rejected.}

\item{where}{Optional extra SQL predicate over the interface's properties.}

\item{params}{Parameters for \code{where}.}
}
\value{
A data frame with the interface's properties plus \code{type_id}.
}
\description{
The ontology's job is to let a question be asked once. "Which named entities
appear in this document" spans Company and Person today and may span more
tomorrow; asking it per type means the list of types lives at the call site,
and a new type silently stops being included in the answer. Asking it by
interface means adding \code{"Named"} to a new object type is enough.
}
\details{
Mirrors \code{objectSetsR::object_set_by_interface()}: the result is projected to
the interface's properties only, so every row has the same shape whichever
type it came from. A \code{type_id} column says which that was.
}
