% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{orph_load_cuad}
\alias{orph_load_cuad}
\title{Load a CUAD-format labelled corpus}
\usage{
orph_load_cuad(path)
}
\arguments{
\item{path}{Path to a CUAD JSON file (e.g. \code{CUADv1.json}).}
}
\value{
A list with \code{contracts} (title, context) and \code{labels} (one row per
labelled span) and \code{categories}.
}
\description{
CUAD ships in SQuAD form: \verb{data[].title}, \verb{paragraphs[].context}, and
\code{qas[]} whose \code{question} names the clause category and whose \code{answers[]}
carry \code{text} and \code{answer_start}.
}
\details{
The category vocabulary is read \strong{from the file}, never hardcoded. CUAD
documents 41 categories; a filtered or extended copy will have a different
set, and a benchmark that silently scored against the wrong vocabulary would
be worse than none.
}
