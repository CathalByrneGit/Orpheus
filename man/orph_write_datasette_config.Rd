% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasette.R
\name{orph_write_datasette_config}
\alias{orph_write_datasette_config}
\title{Write the Datasette configuration file for the store}
\usage{
orph_write_datasette_config(
  path = "inst/datasette/datasette.yml",
  database_name = "orpheus",
  bundle = orph_load_bundle(),
  api_url = "http://127.0.0.1:8000",
  max_file_size = 50 * 1024 * 1024,
  metadata_path = file.path(dirname(path), "metadata.yml")
)
}
\arguments{
\item{path}{Where to write the config YAML. The metadata file is written
beside it as \code{metadata.yml} unless \code{metadata_path} says otherwise.}

\item{database_name}{The name Datasette will serve the database under.}

\item{bundle}{The bundle whose instance tables the generated queries span.
Defaults to the shipped one.}

\item{api_url}{Base URL of the Plumber API the UI plugin should call.}

\item{max_file_size}{Per-file ceiling on browser uploads, in bytes.}

\item{metadata_path}{Where to write the descriptive metadata YAML.}
}
\value{
Invisibly, a named character vector of both paths.
}
\description{
One file rather than several: Datasette takes a single \code{--config}, and the
table descriptions, the \code{allow} blocks, the canned queries and the UI
plugin's settings all have to agree with the bundle and the permission model.
Generating them together is what stops them drifting apart.
}
\details{
The API token is not written here. The file names the environment variable
Datasette should read it from, so regenerating never commits a secret.
}
