% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{orph_llm_json}
\alias{orph_llm_json}
\title{Call a model and parse a JSON reply}
\usage{
orph_llm_json(
  con,
  tier,
  system_prompt,
  user_prompt,
  purpose,
  document_id = NULL,
  actor_id = NULL,
  excerpt_only = FALSE,
  opt_in = FALSE
)
}
\arguments{
\item{con}{A writable connection (for the audit row).}

\item{tier}{\code{"local"} or \code{"cloud"}.}

\item{system_prompt, user_prompt}{Prompt strings.}

\item{purpose}{Short label recorded in the audit log.}

\item{document_id, actor_id}{Recorded in the audit log.}

\item{excerpt_only}{Whether \code{user_prompt} is an excerpt rather than a whole document.}

\item{opt_in}{Explicit cloud opt-in; ignored for the local tier.}
}
\value{
The parsed JSON as a list.
}
\description{
Every call is written to \code{llm_calls} before returning, success or failure,
so the audit log records attempts rather than only outcomes.
}
