% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bundle.R
\name{sync_bundle_aliases}
\alias{sync_bundle_aliases}
\title{Mirror the bundle's alias keys}
\usage{
sync_bundle_aliases(bundle)
}
\arguments{
\item{bundle}{A bundle list.}
}
\value{
The bundle, with every alias matching its primary key.
}
\description{
The three consumer packages read different keys for the same lists:
\code{ontologyDiscoverR} reads \code{object_types}, \code{objectSetsR} reads \code{objects}, and
so on for interfaces, links and concepts. Carrying both spellings is the
price of one bundle serving all three.
}
\details{
It is applied in one place rather than at each call site, because the mirrors
were previously hand-maintained and a schema amendment updated \code{objects}
while leaving \code{interfaceTypes}, \code{links} and \code{concepts} behind.
}
\keyword{internal}
