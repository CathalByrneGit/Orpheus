% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{orph_benchmark_extraction}
\alias{orph_benchmark_extraction}
\title{Benchmark extraction against a CUAD corpus}
\usage{
orph_benchmark_extraction(
  con,
  cuad,
  tier = c("local", "cloud"),
  limit = 5L,
  actor_id = NULL,
  opt_in = FALSE,
  storage_root = "storage/benchmark",
  mapping = orph_load_cuad_map()
)
}
\arguments{
\item{con}{A writable connection.}

\item{cuad}{Output of \code{\link[=orph_load_cuad]{orph_load_cuad()}}.}

\item{tier}{\code{"local"} or \code{"cloud"}.}

\item{limit}{Maximum contracts to run. CUAD is 510 contracts; a full cloud
run is not free, so this defaults to a small sample.}

\item{actor_id}{Actor to attribute the run to.}

\item{opt_in}{Cloud opt-in, required for \code{tier = "cloud"}.}

\item{storage_root}{Where ingested copies are written.}

\item{mapping}{CUAD category to \code{clause_type}.}
}
\value{
A list with per-category results, headline recall, and caveats.
}
\description{
Ingests each contract's text as a document, runs the configured extraction
tier over it, and scores the extracted clauses against CUAD's labelled
spans.
}
