.Rproj.user
.Rhistory
.RData
.Ruserdata
*.sqlite
*.sqlite-wal
*.sqlite-shm
data/
storage/
.env

# Python bytecode from the Datasette plugin
__pycache__/
*.pyc

# Python build and test artefacts
*.egg-info/
build/
dist/
.pytest_cache/
.venv/

# ...except the committed test fixtures
!tests/fixtures/*.sqlite

# The map's frontend. `plugins/static/` is a build artefact -- `npm run build`
# in frontend/ produces it, and the map falls back to a template that needs no
# toolchain when it is absent.
node_modules/
plugins/static/
